
body {
    font-family: 'Roboto', Arial, sans-serif;
    line-height: 1.6;
    color: #1b1a1abe;
    background-color: #b86d11ad;
}

/* Hero Section */
.hero {
    background: #be6908c1 url( photo/pexels-cottonbro-4614249.jpg) ;
    height:auto;

    display:auto;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    padding: 2rem;
    color: #f0edea;
}
.hero img{
        height: 200px;
        width: auto;
        margin-left: 20px;
}
        .hero div{
            max-width: 60px;

        }
    .hero{
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 10px;
    }




.hero h1 {
    font-size: 2.5rem;
    font-weight: bold;
    margin-bottom: 1rem;
}
.hero button {
    padding: 0.8rem 2rem;
    font-size: 1rem;
    background-color: #c760119e;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
}
.hero button:hover {
    background-color: #d9414100;
}

/* Products Section */
.products {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
    padding: 2rem;
    background-color: #fff;
}
.product-item {
    text-align: center;
}
.product-item img {
    width: 100%;
    max-width: 250px;
    height: auto;
    margin-bottom: 1rem;
}
.product-item h3 {
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
}

/* Retail Locator Section */
.retail-locator {
    display: flex;
    flex-wrap: wrap;
    padding: 2rem;
    background-color: #333;
    color: white;
}
.locator-content {
    flex: 1;
    padding: 1rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    text-align: center;
}
.locator-content h2 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
}
.locator-content button {
    padding: 0.8rem 2rem;
    font-size: 1rem;
    background-color: #c760119e;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;

}

.locator-content button:hover {
    background-color: #d94141;
}
.locator-map {
    flex: 1;
    padding: 1rem;
}
.locator-map img {
    width: 100%;
    height: auto;
    border-radius: 5px;
}

/* Carousel Section */


 .carousel {
    background-color: #fff;
    padding: 2rem;
    position: relative;
    max-width: 100%;
    overflow: hidden;
    text-align: center;
 }
    .carousel {background-color: #a979435d;
}
.carousel-track {
    display: flex;
    transition: transform 0.5s ease-in-out;
}
.carousel-slide {
    min-width: 100%;
    box-sizing: border-box;
}
.carousel-slide img {
    width: 100%;
    height: auto;
    display: block;
    margin: 0 auto;
    max-width: 250px;
}


.carousel-slide h3 {
    margin-top: 1rem;
    font-size: 1.2rem;
}
.carousel-buttons {
    position: absolute;
    top: 50%;
    width: 100%;
    display: flex;
    justify-content: space-between;
    transform: translateY(-50%);
}
.carousel-buttons button {
    background-color: rgba(0, 0, 0, 0.5);
    color: rgba(163, 157, 157, 0.178);
    border: none;
    padding: 0.5rem 1rem;
    cursor: pointer;
    font-size: 1.5rem;
}
.carousel-buttons button:hover {
    background-color: rgba(0, 0, 0, 0.8);
}





/* Testimonials Section */
.testimonials {
    background: #4a494949;
    padding: 2rem;
    text-align: center;
}
.testimonials p {
    font-style: italic;
    margin-bottom: 1rem;
    font-size: 1.1rem;
}
.testimonials h4 {
    font-weight: bold;
    font-size: 1rem;
    margin-top: 0.5rem;
}

/* Footer Section */
footer {
    background: #333;
    color: white;
    text-align: center;
    padding: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    align-items: center;
}
footer p {
    margin: 0.3rem 0;
}
footer a {
    color: #f05454;
    text-decoration: none;
}
footer a:hover {
    text-decoration: underline;
}

/* Footer Navigation */
.footer-links {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
}
.footer-links a {
    font-size: 0.9rem;
    color: white;
    transition: color 0.3s;
}
.footer-links a:hover {
    color: #f05454;
}